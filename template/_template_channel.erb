Type <%= new_data['channel_type'] %> : <%= new_data['channelIdentifier'] %> [
        <% if new_data['stateTopic'] %>stateTopic = "<%= new_data['stateTopic'] %>",<% end %>
        <% if new_data['commandTopic'] %>commandTopic = "<%= new_data['commandTopic'] %>",<% end %>
        <% if new_data['postCommand'] %>postCommand = <%= new_data['postCommand'] %>,<% end %>
        <% if new_data['retained'] %>retained = <%= new_data['retained'] %>,<% end %>
        <% if new_data['qos'] %>qos = <%= new_data['qos'] %>,<% end %>
        <% if new_data['transformationPattern'] %>transformationPattern = "<%= new_data['transformationPattern'] %>",<% end %>
        <% if new_data['transformationPatternOut'] %>transformationPatternOut = "<%= new_data['transformationPatternOut'] %>"<% end %>
    ]
    here